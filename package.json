{
    "scripts": {
        "test": "cypress run",
        "run": "cypress run --browser chrome",
        "open": "cypress open",
        "spec": "cypress run --spec",
        "docker:up": "docker compose up kong-ee-database kong-cp",
        "docker:down": "docker compose down"
    },
    "devDependencies": {
        "cypress": "^14.5.0"
    },
    "dependencies": {
        "cypress-mochawesome-reporter": "^3.8.2"
    }
}
